//gap between two successive nodes
var unit=100;
// var xcomp = 0;
// var ycomp = 0;

// functions which determines x and y 
//components of field vectors

function X(x, y) {
    return -y;
}

function Y(x, y) {
    return x;
}

function equsn(){
    // eval('xcomp' + '=' + xinpt.value());
    // console.log(xcomp)
    // eval('ycomp' + '=' + yinpt.value());
    // ycomp = yinpt.value();
}
